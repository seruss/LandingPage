@* Why I'm Different — JSON block *@
<section class="why-stack reveal" id="stack">
    <div class="container">
        <div class="why-stack-layout">
            @* USP JSON *@
            <div class="reveal">
                <h2 class="why-title">Why I'm Different</h2>
                <div class="json-block">
                    <div class="json-comment">// unique_selling_points.json</div>
                    <div class="json-brace">{</div>
                    <div class="json-line">
                        <span class="json-key">"performance_mindset"</span>:
                        <span class="json-value">"I write SIMD in production when standard libraries aren't fast enough"</span>,
                    </div>
                    <div class="json-line">
                        <span class="json-key">"tb_scale"</span>:
                        <span class="json-value">"Built a 3TB/day logging platform from scratch — not just used one"</span>,
                    </div>
                    <div class="json-line">
                        <span class="json-key">"early_adopter"</span>:
                        <span class="json-value">"Integrated GPT-3.5 in production before the hype (2019)"</span>,
                    </div>
                    <div class="json-line">
                        <span class="json-key">"ownership"</span>:
                        <span class="json-value">"I don't just write code — I own design, deployment, and the SLA"</span>
                    </div>
                    <div class="json-brace">}</div>
                </div>
            </div>

            @* Tech Stack *@
            <div class="reveal">
                <TechStackSection />
            </div>
        </div>
    </div>
</section>
