@* Why I'm Different — JSON block *@
<section class="why-different-section reveal" id="stack">
    <div class="container">
        <div class="reveal">
            <h2 class="section-heading">Why I'm Different</h2>
            <div class="section-heading-bar"></div>
            <div class="json-block">
                <div class="json-comment">// unique_selling_points.json</div>
                <div class="json-brace">{</div>
                <div class="json-line">
                    <span class="json-key">"performance"</span>:
                    <span class="json-value">"When it matters, I go below the framework — SIMD, memory layout, zero-copy paths"</span>,
                </div>
                <div class="json-line">
                    <span class="json-key">"scale"</span>:
                    <span class="json-value">"Built and operated systems processing terabytes per day — scaling was designed, not discovered"</span>,
                </div>
                <div class="json-line">
                    <span class="json-key">"ai"</span>:
                    <span class="json-value">"Integrated LLMs before hype cycles — focusing on automation value, not demos"</span>,
                </div>
                <div class="json-line">
                    <span class="json-key">"ownership"</span>:
                    <span class="json-value">"I own systems end-to-end: design, deployment, on-call, SLAs — no hand-offs, no excuses"</span>,
                </div>
                <div class="json-line">
                    <span class="json-key">"reliability"</span>:
                    <span class="json-value">"Retries, backpressure, idempotency, and recovery are default — not add-ons"</span>
                </div>
                <div class="json-brace">}</div>
            </div>
        </div>
    </div>
</section>

@* Tech Stack *@
<section class="tech-stack-section reveal">
    <div class="container">
        <div class="reveal">
            <h2 class="section-heading">Tech Stack</h2>
            <div class="section-heading-bar"></div>
            <TechStackSection />
        </div>
    </div>
</section>
